name: big_decimal
description: Implementação próxima do equivalente ao BigDecimal em Java, adaptada para Dart.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  decimal: ^1.3.0

dev_dependencies:

